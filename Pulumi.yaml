name: git_project
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Pulumi codes using Python and YAML pl.
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
